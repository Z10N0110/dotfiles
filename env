## top env vars
# http proxys
_https_proxy='http://127.0.0.1:4411'
_http_proxy='http://127.0.0.1:4411'
# http_proxy_short_var
hpsv='http_proxy='$_http_proxy' https_proxy='$_https_proxy''

# alias
[[ -f ~/.alias ]] && source ~/.alias

# golang envs
[[ -f ~/.goenv ]] && source ~/.goenv

# pyenv
[[ -f ~/.pyenv ]] && source ~/.pyenv
